<nav class="bg-white shadow-md fixed top-0 w-full z-10 font-Poppins">
  <div class="container mx-auto flex justify-between items-center p-4">
    <!-- Logo -->
    <div class="text-2xl font-bold text-indigo-600">ConversApp</div>

    <!-- Menu -->
    <div class="hidden md:flex space-x-6 text-gray-700 font-medium items-center">
      <a href="#home" class="hover:text-indigo-600 transition">Home</a>
      <a href="#services" class="hover:text-indigo-600 transition">Services</a>
      <a href="#contact" class="hover:text-indigo-600 transition">Contact</a>
      <button
        data-modal-target="authentication-modal"
        class="bg-indigo-700 text-white px-4 py-2 rounded-lg font-medium hover:bg-indigo-800 transition"
      >
        Sign In
      </button>
    </div>

    <!-- Mobile Menu Button -->
    <button
      id="menu-toggle"
      aria-label="Open Menu"
      class="block md:hidden text-gray-700 focus:outline-none"
    >
      <svg
        class="w-6 h-6"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16m-7 6h7"
        />
      </svg>
    </button>
  </div>

  <!-- Mobile Menu -->
  <div
    id="mobile-menu"
    class="hidden md:hidden flex flex-col space-y-4 p-4 bg-white shadow-md"
  >
    <a href="#home" class="hover:text-indigo-600 transition">Home</a>
    <a href="#services" class="hover:text-indigo-600 transition">Services</a>
    <a href="#contact" class="hover:text-indigo-600 transition">Contact</a>
    <button
      data-modal-target="authentication-modal"
      class="bg-indigo-700 text-white px-4 py-2 rounded-lg font-medium hover:bg-indigo-800 transition"
    >
      Sign In
    </button>
  </div>
</nav>

<!-- Modal -->
<div
  id="authentication-modal"
  tabindex="-1"
  aria-hidden="true"
  class="fixed inset-0 z-50 hidden bg-gray-900 bg-opacity-50 flex justify-center items-center"
>
  <div class="bg-white rounded-lg shadow-lg max-w-md w-full p-6 relative">
    <button
      id="modal-close"
      class="absolute top-3 right-3 text-gray-500 hover:text-gray-700"
    >
      <svg
        class="w-5 h-5"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M6 18L18 6M6 6l12 12"
        />
      </svg>
    </button>
    <h2 class="text-lg font-semibold text-gray-700 text-center mb-4">
      Sign In to ConversApp
    </h2>
    <form class="space-y-4">
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
        <input
          type="email"
          id="email"
          class="w-full mt-1 p-2 border rounded-lg focus:ring-indigo-500 focus:border-indigo-500"
          placeholder="you@example.com"
          required
        />
      </div>
      <div>
        <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
        <input
          type="password"
          id="password"
          class="w-full mt-1 p-2 border rounded-lg focus:ring-indigo-500 focus:border-indigo-500"
          placeholder="••••••••"
          required
        />
      </div>
      <button
        type="submit"
        class="w-full bg-indigo-700 text-white px-4 py-2 rounded-lg hover:bg-indigo-800 transition"
      >
        Sign In
      </button>
      <div class="text-center text-sm mt-4">
        <a href="#" class="text-indigo-600 hover:underline">Forgot your password?</a> | 
        <a href="#" class="text-indigo-600 hover:underline">Create an account</a>
      </div>
    </form>
  </div>
</div>

<script is:inline>
  // Toggle mobile menu
  document.getElementById("menu-toggle").addEventListener("click", () => {
    const mobileMenu = document.getElementById("mobile-menu");
    mobileMenu.classList.toggle("hidden");
  });

  // Toggle modal visibility
  document.querySelectorAll("[data-modal-target]").forEach((btn) => {
    btn.addEventListener("click", () => {
      const modal = document.getElementById("authentication-modal");
      modal.classList.remove("hidden");
    });
  });

  // Close modal on X or outside click
  const modal = document.getElementById("authentication-modal");
  const closeModal = () => modal.classList.add("hidden");
  document.getElementById("modal-close").addEventListener("click", closeModal);
  window.addEventListener("click", (event) => {
    if (event.target === modal) closeModal();
  });
</script>
